import com.sxexesx.models.Store

dialect  "mvel"

rule "Calculate Store Rating"
    when
        storeInstance: Store(isActive == true);
    then
        System.out.println("This Store is Active");
end