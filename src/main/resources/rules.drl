import com.sxexesx.model.Store
import com.sxexesx.model.ResultStorage

dialect  "mvel"

rule "Calculate Store Rating"
    when
        $s: Store(isActive == true)
    then

        System.out.println("This Store is Active");
end